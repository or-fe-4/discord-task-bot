# Discord Developer Portal > Bot > Token
DISCORD_TOKEN=your_discord_bot_token_here

# https://aistudio.google.com/apikey で取得
GEMINI_API_KEY=your_gemini_api_key_here

# 使うGeminiモデル。安さ優先なら flash-lite、多少賢さが欲しければ flash
GEMINI_MODEL=gemini-2.5-flash-lite

# Botに喋らせる/タスク通知するチャンネルのID
# チャンネルを右クリック→「IDをコピー」(開発者モードをオンにする必要あり)
CHANNEL_ID=123456789012345678

# 毎日タスク一覧を通知する時刻 (JST, 24時間表記)
NOTIFY_HOUR=6
NOTIFY_MINUTE=0

# Botの口調・性格。ここを書き換えるだけでキャラが変わる
PERSONA_PROMPT="あなたはユーザーの個人アシスタント兼相棒のような存在です。フレンドリーだけど馴れ馴れしすぎない、少しくだけたタメ口で話してください。絵文字は使っても1つまで。長々とした説明は避け、2〜3文程度で簡潔に返してください。"
